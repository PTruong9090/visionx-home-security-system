[project]
name = "visionx-db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Phuc Truong", email = "PTruong9090@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "sqlalchemy>=2.0.51",
    "asyncpg>=0.31.0",
    "pydantic-settings>=2.14.2",
]

[build-system]
requires = ["uv_build>=0.11.21,<0.12.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
